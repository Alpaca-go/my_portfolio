# 2026-07-24 工作日志

## 修复：返回文件夹动画顺序（两端先渐出，中间后收起）
- 问题：首页文件夹点击进入卡片轮播区后，返回时中间3张（文件夹飞出的 inner-left/center/inner-right）与两端卡（outer-left/outer-right）同时动作，且两端是 0ms 硬切消失，顺序与预期（两端先渐出 → 中间3张再收起）相反。
- 改动：
  - `src/App.jsx` `handleDetailBack`：拆成两阶段。阶段A 设 `is-closing` 让两端卡渐出；阶段B 延迟 `edgeFadeMs=240` 后才 `setDetailCardsTransitioning(true)` 触发中间3张飞回。清理 timer 顺延到 `edgeFadeMs + 700`。
  - `src/styles.css`：`.brand-detail.is-closing .brand-detail__fan-card--edge` 由 0ms 硬切改为 `transition: opacity 240ms ease`（平滑渐出）。
  - `src/styles.css`：新增 `.brand-detail.is-closing:not(.is-card-transitioning) .brand-detail__fan-card--motion-target { opacity:1; transition: opacity 0ms }`，避免阶段A 时中间3张被 `is-closing` 的 0ms 硬切规则提前隐藏。
- 进入动画（3张飞出→两端再出现）顺序原本正确，本次仅修返回逆序。
- 验证：Vite HMR 更新成功，无编译错误。dev server 在 http://localhost:5173/ 运行中。

## 撤销：15:55 用户要求"返回上一步"
- 已撤销上述返回动画修复的全部三处改动，恢复 `src/App.jsx` 与 `src/styles.css` 至修改前状态（返回时两端卡 0ms 硬切、中间3张与两端同时飞回的原始行为）。
- 当前代码状态：与最初进入前的版本一致。如需重新处理该动画顺序问题，可参考上方修复方案。

## 修复：返回文件夹动画顺序 — 第二版（16:05）
- 需求：返回时两端卡先渐出，中间3张保持可见并按飞出路径原路返回到文件夹。
- 根因：
  1. `.brand-detail.is-closing .brand-detail__fan-card` 让所有 fan-card（含中间3张）0ms 硬切消失，导致原生卡片先消失、过渡层再"凭空出现"飞回，视觉上不是连续的原路返回。
  2. JS 中 `handleDetailBack` 没有分阶段：is-closing、is-card-transitioning、transitionActive 几乎同时触发。
- 改动：
  - `src/styles.css`：新增 `.brand-detail.is-closing:not(.is-card-transitioning) .brand-detail__fan-card--motion-target { opacity:1; transition: opacity 0ms }`，确保阶段A（两端渐出期间）中间3张保持可见；`.brand-detail.is-closing .brand-detail__fan-card--edge` 已有 `opacity 300ms ease` 渐出（会话前本地修改）。
  - `src/App.jsx` `handleDetailBack`：拆为两阶段。阶段A（0-300ms）仅设 `is-closing`，edge 渐出、motion-target 保持可见；阶段B（300ms）设 `detailVisible=false` + `is-card-transitioning` + 启动过渡层飞回；清理顺延到 1000ms。`edgeFadeMs = 300` 与 CSS 同步。
- 验证：Vite HMR 编译通过，dev server 在 http://localhost:5174/ 运行。

## Git 重置：返回到"卡片交互"提交（17:15）
- 用户要求返回到 `b0a3e55 卡片交互` 提交。
- 操作：`git branch backup-2026-07-24` 创建备份分支保存重置前状态 → `git reset --hard b0a3e55` 硬重置。
- 结果：HEAD 现在位于 `b0a3e55 卡片交互`，工作区干净。后续提交 `b05efc9 "1"` 被丢弃（保留在备份分支 `backup-2026-07-24` 中）。
- 注意：`.workbuddy/` 目录因被 git 跟踪，在 reset 时被移除，已从备份分支手动恢复。

## 修复：响应式布局问题（17:20）
- 问题1：浏览器宽度变窄（约1280px）时三个文件夹被截掉。
  - 根因：`styles.css` 中 `.page-canvas { overflow: hidden }` 在缩放后可能错误裁切子元素边界。
  - 改动：`src/portfolio-overrides.css` 中 `.page-canvas` 添加 `overflow: visible`，覆盖 `styles.css` 的 `overflow: hidden`。
- 问题2：点击文件夹卡片飞出后，整个页面内容区会下沉一截。
  - 根因：`.page-shell.is-home .page-canvas { --effective-stage-top: 0px }` 仅对首页生效；点击文件夹后 `page-shell` 变为 `has-detail`，置顶规则失效，canvas 随 `stage-top` 垂直居中下移。
  - 改动：`src/portfolio-overrides.css` 新增 `.page-shell.has-detail .page-canvas { --effective-stage-top: 0px }`，让详情页状态下 canvas 也保持置顶。
- 验证：Vite HMR 编译通过，dev server 在 http://localhost:5174/ 运行。

## 修复：轮播区文件夹隐藏与卡片截断（17:45）
- 问题1：进入轮播区（Brand Design 等详情页）后，背景3个文件夹仍然可见。
  - 根因：`page-canvas` 为 `overflow: visible`，且 `CollectionShowcase` 未在 `has-detail` 状态下隐藏。
  - 改动：`src/portfolio-overrides.css` 新增 `.page-shell.has-detail .collection-showcase { opacity:0; visibility:hidden; pointer-events:none; transition: opacity 300ms ease, visibility 300ms }`，进入详情页时文件夹渐隐；同时 `.page-shell.has-detail .page-canvas { overflow: hidden }`，超出边界的卡片被截断。
- 问题2：轮播区中间3张卡片应像两端卡片一样被截掉。
  - 根因：`brand-detail__coverflow` 为 `overflow: visible`，卡片溢出容器后仍可见。
  - 改动：`src/portfolio-overrides.css` 中 `.brand-detail__coverflow` 的 `overflow` 从 `visible` 改为 `hidden`。
- 验证：Vite HMR 编译通过，dev server 在 http://localhost:5174/ 运行。

## 修复：返回时文件夹立刻渐入出现（18:05）
- 问题：收起卡片回到文件夹时，对应的文件夹出现太晚（等卡片飞回完成后才出现）。
- 根因：`.page-shell.has-detail .collection-showcase { opacity: 0 }` 让文件夹在整个返回动画期间都隐藏，`hasDetail` 直到动画结束（closingKey 清除）才变为 false。
- 改动：
  - `src/App.jsx`：新增 `isReturning = Boolean(closingKey)`，给 `page-shell` 添加 `is-returning` 类。
  - `src/portfolio-overrides.css`：新增 `.page-shell.has-detail.is-returning .collection-showcase { opacity: 1; visibility: visible }`，让返回动画期间文件夹立刻可见（而非等动画结束）。进入动画期间（无 is-returning）文件夹仍保持隐藏。
- 验证：Vite HMR 编译通过，dev server 在 http://localhost:5174/ 运行。
